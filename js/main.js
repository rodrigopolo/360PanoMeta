'use strict';

// -- Boot ---------------------------------------------------------------------
// All feature logic lives in: state.js, calc.js, rotation.js, map.js,
// viewer.js, datetime.js, exif.js, command.js, upload.js (loaded before this
// file — see index.html for order).
bindControls();
